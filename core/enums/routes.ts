export enum PublicRoute {
  Home = '/',
  Login = '/login',
  Logout = '/logout',
}

export enum Route {
  Timeline = '/timeline',
  Search = '/search',
  Dig = '/dig',
  Profile = '/profile',
  AddCrate = '/create',
  VerifyEmail = '/verify-email',
  NewProfile = '/new-profile',
}
