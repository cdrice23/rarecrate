export enum PublicRoute {
  Home = '/',
  Login = '/login',
  Logout = '/logout',
}

export enum Route {
  Timeline = '/timeline',
  Search = '/search',
  Dig = '/dig',
  Profile = '/profile',
  AddCrate = '/create',
  NewProfile = '/new-profile',
}
