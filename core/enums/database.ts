enum Role {
  SUPERADMIN,
  USER,
}

enum RequestStatus {
  PENDING,
  ACCEPTED,
  REJECTED,
}

enum SocialPlatform {
  TWITTER,
  INSTAGRAM,
  SPOTIFY,
  YOUTUBE,
  DISCOGS,
}

enum SearchModel {
  ALBUM,
  CRATE,
  PROFILE,
  LABEL,
  TAG,
}
