export enum Role {
  SUPERADMIN,
  USER,
}

export enum RequestStatus {
  PENDING,
  ACCEPTED,
  REJECTED,
}

export enum SocialPlatform {
  TWITTER,
  INSTAGRAM,
  SPOTIFY,
  YOUTUBE,
  DISCOGS,
}

export enum SearchModel {
  ALBUM,
  CRATE,
  PROFILE,
  LABEL,
  TAG,
}
